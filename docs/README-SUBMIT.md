# 队长提交清单（10 行）

1. 团队名称填「拒做韭菜」，队长填梁锐文，公司写恩筑AI / NGJOO；参赛场景只选“场景 1：AI 智能上新”。
2. 方案名称使用“跨境上架编译器”。
3. 从 `docs/SUBMISSION.md` 复制官方字段内容，并在网页预览后复核格式。
4. 方案长稿以 `docs/PROPOSAL.md` 为准，短文本以文档仓 `submit/` 两个 TXT 为准。
5. 附加材料优先填可测 Demo：https://returned-instructors-abc-winston.trycloudflare.com ；截图作备份。
6. 再上传 `docs/attachments/01-sku-input.png`、`02-three-results.png`、`04-promo-conflict.png`，视名额加 `docs/flow.png` 与 `docs/architecture.png`。
7. 演示口径固定为 rules.yaml、checker、三级降级、tldraw、两类节点。
8. 明确图片走兼容 `/images/generations` 并回传 data URL，checker 不验像素。
9. 明确投放下载物是 TXT 脚本，产品不自动上架，也不担保审核结果。
10. 提交前检查压缩包中没有 `.env`、Key、无关仓库代码或内部服务配置。
